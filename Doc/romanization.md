There are 2 schemes of spelling with the roman alphabet
 - Romanian already uses the roman alphabet
 - Slavic languages (Bulgarian, Macedonian, Serbian)
    - use of schwa `ə`
    - `c` is a `ts` sound
    - `č` is a `ch` sound
